# Scripts

> Setup, deployment, and utility scripts.

## Naming Convention

- `setup-*.sh` — one-time setup scripts
- `deploy-*.sh` — deployment scripts
- `util-*.sh` — utility and helper scripts

## Usage

Scripts are also accessible via the Makefile:
```bash
make setup    # runs setup scripts
make deploy   # runs deployment scripts
```
